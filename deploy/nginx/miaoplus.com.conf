server {
  listen 80;
  server_name miaoplus.com www.miaoplus.com;

  root /var/www/miaoplus.com/dist;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /assets/ {
    expires 30d;
    add_header Cache-Control "public, immutable";
  }
}
